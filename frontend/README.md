# Bookstore Recommendation Frontend

Современный frontend на React 18 + Vite для книжного магазина с персональными рекомендациями.

## 🚀 Стек
- React 18 + Vite
- React Router v6
- Tailwind CSS
- TanStack Query
- Axios
- Zustand
- React Hook Form + Zod
- Lucide React и React Hot Toast

## ⚙️ Запуск проекта
`ash
# установка зависимостей
npm install

# запуск в режиме разработки
npm run dev

# проверка линтера
npm run lint

# production-сборка
npm run build

# предпросмотр production-сборки
npm run preview
`

Приложение стартует на http://localhost:5173. API проксируется на http://localhost:8000 (можно изменить в .env).

## 🌐 Переменные окружения
Скопируйте .env.example в .env и при необходимости измените значения:

`env
VITE_API_URL=http://localhost:8000
VITE_API_TIMEOUT=15000
`

## 🧱 Структура проекта
`
frontend/
├── public/
├── src/
│   ├── api/              # Клиенты для backend API
│   ├── components/       # UI-компоненты (layout, книги, auth, рекомендации, общее)
│   ├── hooks/            # Хуки для работы с данными (auth, книги, рекомендации, взаимодействия)
│   ├── pages/            # Страницы приложения
│   ├── store/            # Zustand store
│   ├── utils/            # Константы и хелперы
│   ├── App.jsx           # Маршруты
│   └── main.jsx          # Точка входа
├── .env.example
├── tailwind.config.js
├── vite.config.js
└── README.md
`

## ✨ Основные возможности
- Авторизация и регистрация с JWT (Zustand + React Query)
- Персональные рекомендации, популярные книги, новинки
- Каталог с фильтрами, поиском, пагинацией
- Страница книги с похожими рекомендациями и отслеживанием взаимодействий
- Профиль пользователя с историей, редактированием предпочтений
- Адаптивный дизайн (Tailwind, responsive layout)
- Уведомления и обработка ошибок

## 🔗 Полезные команды
- 
pm run lint — проверка ESLint
- 
pm run build && npm run preview — production билд и предпросмотр

## 📄 Примечания
- Прокси настроен в ite.config.js (/api → VITE_API_URL)
- Маршруты защищаются компонентом ProtectedRoute (перенаправление на /login при 401)
- Все API-запросы проходят через src/api/axios.js с автоматическим добавлением токена и обработкой 401

Удачной разработки! 🎉
